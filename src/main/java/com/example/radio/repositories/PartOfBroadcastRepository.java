package com.example.radio.repositories;

import com.example.radio.entities.PartOfBroadcast;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartOfBroadcastRepository extends JpaRepository<PartOfBroadcast, Integer> {
}